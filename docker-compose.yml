services:
  app:
    image: node:24.14.0-slim
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3300:3000"
    environment:
      - NODE_ENV=development
    command: sh -c "npm install && npx next dev"
